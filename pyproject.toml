[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vectorbt-project"
version = "0.1.0"
description = "基于 VectorBT 的金融数据处理与回测系统"
readme = "readme.md"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
include = ["*"]

